<Window
    x:Class="HOI_Error_Tools.View.ErrorFileInfoView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:HOI_Error_Tools.View"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:HOI_Error_Tools.ViewModels"
    Title="ErrorFileInfoView"
    Width="800"
    Height="450"
    d:DataContext="{d:DesignInstance vm:ErrorFileInfoViewModel}"
    mc:Ignorable="d">
    <Grid>
        <DataGrid
            AutoGenerateColumns="False"
            IsReadOnly="True"
            ItemsSource="{Binding Data}">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding FilePath}" Header="文件路径" />
                <DataGridTextColumn Binding="{Binding ErrorLine}" Header="行号" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
