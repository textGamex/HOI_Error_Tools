<Window
    x:Class="HOI_Error_Tools.View.SettingsWindowView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:HOI_Error_Tools.View"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vms="clr-namespace:HOI_Error_Tools.ViewModels"
    Title="SettingsWindowView"
    Width="800"
    Height="450"
    d:DataContext="{d:DesignInstance vms:SettingsWindowViewModel}"
    mc:Ignorable="d">

    <TabControl ItemsSource="{Binding Data, Mode=Default}">
        <i:Interaction.Triggers>
            <i:EventTrigger EventName="SelectionChanged">
                <i:InvokeCommandAction Command="{Binding ControlSelectionChangedCommand, Mode=Default}" PassEventArgsToCommand="True" />
            </i:EventTrigger>
        </i:Interaction.Triggers>
    </TabControl>
</Window>
